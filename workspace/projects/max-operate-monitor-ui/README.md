# max-operate-monitor-ui Mounted Project / max-operate-monitor-ui 挂载项目

## 中文

独立 T-MAX 项目，公共背景为 `xiaoneng`，只挂载 `max-operate-monitor-ui`。运行 `npm run mount:max-operate-monitor-ui` 创建本机挂载。

## English

Standalone T-MAX project using `xiaoneng` as its shared background and mounting only `max-operate-monitor-ui`. Run `npm run mount:max-operate-monitor-ui` to create local mounts.
