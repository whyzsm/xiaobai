# max-operate-monitor-ui Project Skill / max-operate-monitor-ui 项目技能

## 中文

`max-operate-monitor-ui` 是独立 T-MAX 业务项目，只挂载同名业务仓，并使用 `xiaoneng` 作为公共背景。修改前读取背景、检查目标仓状态，业务代码只落在目标仓；默认只做本地修改和静态检查。

## English

`max-operate-monitor-ui` is a standalone T-MAX business project. It mounts only the repository with the same name and uses `xiaoneng` as its shared background. Read the background and inspect the target repository before editing; business code belongs only in that repository, with local edits and static checks as the default.
