# scan Mounted Project / scan 挂载项目

## 中文

独立 T-MAX 项目，公共背景为 `xiaoneng`，只挂载 `scan`。运行 `npm run mount:scan` 创建本机挂载。

## English

Standalone T-MAX project using `xiaoneng` as its shared background and mounting only `scan`. Run `npm run mount:scan` to create local mounts.
