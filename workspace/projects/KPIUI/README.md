# KPIUI Mounted Project / KPIUI 挂载项目

## 中文

独立 T-MAX 项目，公共背景为 `xiaoneng`，只挂载 `KPIUI`。运行 `npm run mount:KPIUI` 创建被忽略的本机挂载。

## English

Standalone T-MAX project using `xiaoneng` as its shared background and mounting only `KPIUI`. Run `npm run mount:KPIUI` to create ignored local mounts.
