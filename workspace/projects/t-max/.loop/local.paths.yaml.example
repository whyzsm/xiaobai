# local.paths.yaml is ignored by git because paths are machine-specific.
# The t-max group no longer declares a background (Batch E removed the legacy
# Xiaoneng route) and owns no repositories; this file remains because the mount
# script reads it. Standalone T-MAX projects keep their own local.paths.yaml.
background: {}
