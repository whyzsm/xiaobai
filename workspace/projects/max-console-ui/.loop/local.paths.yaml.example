# Copy to local.paths.yaml and replace the machine-local paths.
# 复制为 local.paths.yaml 后，替换为本机绝对路径。
background:
  xiaoneng: /absolute/path/to/xiaoneng
repositories:
  max-console-ui: /absolute/path/to/T-MAX/max-console-ui
